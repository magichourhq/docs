export const ToolSection = ({ type, outputs, title, productSlug, apiSlug }) => (
  <>
    <Tabs>
      {outputs.map((output) => (
        <Tab title="Example Output">
          <Frame>
            {type === "video" ? (
              <video
                src={output.src}
                muted
                playsInline
                controls
                className="rounded-lg h-80"
                loop
              />
            ) : (
              <img height="320" className="rounded-lg h-80" src={output.src} />
            )}
          </Frame>
        </Tab>
      ))}
    </Tabs>

    <CardGroup cols={2}>
      <Card
        title="Product Page"
        icon="video"
        horizontal
        href={`https://magichour.ai/products/${productSlug}`}
      >
        Learn more about {title}
      </Card>
      <Card
        title="API Spec"
        icon="webhook"
        horizontal
        href={`/api-reference/${type}-projects/${apiSlug}`}
      >
        See API details
      </Card>
    </CardGroup>

  </>
);
