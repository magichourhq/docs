export const ToolSection = ({ 
  type = "image", 
  outputs = [], 
  title = "", 
  productSlug = "", 
  apiSlug = "" 
}) => (
  <>
    <CardGroup cols={2}>
      <Card
        title="Create with the API"
        horizontal
        href={`/api-reference/${type}-projects/${apiSlug}`}
      >
        Required inputs, models, and request examples
      </Card>
      <Card
        title="Try in the web app"
        horizontal
        href={`https://magichour.ai/products/${productSlug}${productSlug.includes("?") ? "&" : "?"}utm_source=docs&utm_medium=referral&utm_campaign=tools`}
      >
        Open {title} without writing code
      </Card>
    </CardGroup>

    <p>New to the API? <a href="/get-started/quick-start">Set up your API key and SDK</a>, then use the code examples below. For local files, <a href="/integration/inputs-and-outputs">upload the bytes first</a> and pass the returned file path.</p>

    {outputs && outputs.length > 0 && (
      <Tabs>
        {outputs.map((output, idx) => (
          <Tab key={idx} title={`Example Output ${idx + 1}`}>
            <Frame>
              {type === "video" ? (
                <video
                  controls
                  preload="metadata"
                  playsInline
                  className="rounded-lg h-80"
                  src={`${output.src}#t=0.001`}
                  type={`${output.src?.endsWith("mp4") ? 'video/mp4': "video/webm"}`}
                >
                </video>
              ) : type === "audio" ? (
                <audio
                  controls
                  preload="metadata"
                  className="w-full"
                  src={output.src}
                >
                  Your browser does not support the audio element.
                </audio>
              ) : (
                <img
                  alt={`${title} example output`}
                  height="320"
                  className="rounded-lg h-80"
                  src={output.src}
                />
              )}
            </Frame>
          </Tab>
        ))}
      </Tabs>
    )}

  </>
);
